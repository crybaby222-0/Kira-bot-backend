{
  "name": "kira-root",
  "private": true,
  "scripts": {
    "build": "npm install --prefix kira-backend && npx prisma generate --schema=kira-backend/prisma/schema.prisma && npx prisma db push --schema=kira-backend/prisma/schema.prisma && npm run build --prefix kira-backend",
    "start": "npm run start --prefix kira-backend"
  }
}
